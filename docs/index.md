---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "langfuse Provider"
subcategory: ""
description: |-
  The Langfuse provider allows managing Langfuse resources via the Langfuse API.
---

# langfuse Provider

The Langfuse provider allows managing Langfuse resources via the Langfuse API.

## Example Usage

```terraform
terraform {
  required_providers {
    langfuse = {
      source = "registry.terraform.io/kenzo0107/langfuse"
    }
  }
}

provider "langfuse" {
  # public_key = var.langfuse_public_key  # or LANGFUSE_PUBLIC_KEY env var
  # secret_key = var.langfuse_secret_key  # or LANGFUSE_SECRET_KEY env var
  # host       = "https://cloud.langfuse.com"  # or LANGFUSE_HOST env var
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `host` (String) Langfuse API host. May also be provided via `LANGFUSE_HOST` environment variable. Defaults to `https://cloud.langfuse.com`.
- `public_key` (String, Sensitive) Langfuse public API key. May also be provided via `LANGFUSE_PUBLIC_KEY` environment variable.
- `secret_key` (String, Sensitive) Langfuse secret API key. May also be provided via `LANGFUSE_SECRET_KEY` environment variable.
